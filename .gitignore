# Godot 4 导入缓存与本地状态
.godot/
/android/

# 导出产物
/build/
/export/
*.tmp

# macOS
.DS_Store

# 编辑器本地配置
*.translation

# API key，绝不提交
.ark_key
# codex -s workspace-write 会留下的空文件
.copy-placeholder

# 第一章原文的中间产物，本地用 tools/cut_scenes.py 生成，不进仓库
data/scenes_ch01.json
